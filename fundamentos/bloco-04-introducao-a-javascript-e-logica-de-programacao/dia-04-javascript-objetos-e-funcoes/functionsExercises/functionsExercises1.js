// Para esses exercícios, você deve refazer os exercícios de 1 a 5 do dia 4.1 e fazer com que eles sejam funções de um mesmo arquivo.

// 1 - Fazer cinco programas, um para cada operação aritmética básica. O programa deve ter duas constantes, 'a e b', definidas no começo com os valores que serão operados. Fazer programas para:

// Adição (a + b)

function sum(a, b) {
  return a + b;
}

console.log('O resultado dessa soma é: ', soma(0,0));

// Subtração (a - b)

function sub(a, b) {
  return a - b;
}

console.log('O resultado dessa subtração é: ', sub(0,0));

// Multiplicação (a * b)

function mult(a, b) {
  return a * b;
}

console.log('O resultado dessa multiplicação é: ', mult(0,0));

// Divisão (a / b)

function div(a, b) {
  return a / b;
}

console.log('O resultado dessa divisão é: ', div(0,0));

// Módulo (a % b)

function rem(a, b) {
  return a % b;
}

console.log('O resto dessa divisão é: ', rem(0,0));
