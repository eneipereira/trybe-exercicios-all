// 1 - Criar uma constante chamada 'base' e uma variável chamada 'height' e atribuir os seus respectivos valores: 5 e 8.

const base = 5;
let height = 8;

// 2 - Criar uma constante chamada 'area' e atribuir a ela o resultado da multiplicação da base pela altura.

const area = base * height;
console.log(area);

// 3 - Criar uma constante chamada 'perimeter' e atribuir a ela a soma de todos os lados do retângulo.

const perimeter = base * 2 + height * 2;
console.log(perimeter);